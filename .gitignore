__pycache__/
*.py[cod]

.venv/
venv/
env/

.DS_Store

# Local source material and generated extraction output
out/
outputs*/
*.egg-info/
*.pdf
*.jsonl

# Project-local opencode config and agent scaffolding (non-skill)
opencode.json
opencode.jsonc
.opencode/agents/
.opencode/commands/
.opencode/node_modules/
.opencode/package.json
.opencode/package-lock.json
.opencode/.gitignore
CLAUDE.md

# Generic engineering skills not part of this corpus-extraction package
# (kept locally for the harness, not distributed with the package)
.opencode/skills/code-philosophy/
.opencode/skills/frontend-philosophy/
.opencode/skills/code-review/

# Ralph loop (local)
ralph.sh
.ralph-config.json

# Local tooling and test artifacts
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# Python build artifacts (regenerated by CI on publish)
dist/
build/

# Ralph loop artifacts
.ralph/
ralph.log

# Development-only files (not part of the shipped product)
# Kept locally for working on the repo, but not tracked/distributed.
AGENTS.md
CONTRIBUTING.md
docs/agents/
scripts/test_extract.py
scripts/requirements.txt
